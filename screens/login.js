import React from 'react';
import Typography from '@mui/material/Typography';

const LoginScreen = () => {
  return <Typography variant="h2">Inicio de Sesión / Registro</Typography>;
};

export default LoginScreen;
